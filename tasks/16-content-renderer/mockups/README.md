# Content renderer mockup

Use `content-renderer-reference.html` as the typography and content-rendering reference for:
- `/posts/:slug`
- `/projects/:slug`

It shows the intended handling of headings, paragraphs, lists, quotes, code blocks and inline images in zero-design style.
