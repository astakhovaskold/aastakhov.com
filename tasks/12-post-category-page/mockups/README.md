# Post category page mockup

No separate HTML mockup was created for `/posts/category/:slug`.
Use the posts index visual structure and filter the list by category.

For `/posts/category/cases`, title should be `Case notes`, not `Cases` as an agency-style section.
