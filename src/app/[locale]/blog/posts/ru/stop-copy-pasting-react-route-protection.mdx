---
title: "Защита роутов в React без копипасты. Как я это решил."
publishedAt: "2026-05-11"
summary: "На каждом проекте нужно защищать роуты. Логика всегда одна. Реализация всегда разная. react-protected стандартизирует это."
tag: "Open Source"
---

На каждом проекте нужно защищать роуты. Одни — только для авторизованных пользователей, другие — для администраторов, третьи — только для гостей. Пишете `PrivateRoute`. Потом хук с проверкой роли. Потом компонент `HasAccess`. Переходите на следующий проект — и пишете всё заново.

Логика всегда одна. Реализация всегда разная.

`react-protected` стандартизирует это. Описываете что нужно каждому роуту — библиотека проверяет доступ и делает редирект.

## Два пакета

`@react-protected/core` — логика проверки доступа без зависимостей от React и любого роутера.

`@react-protected/react-router` — адаптер для React Router. Поддерживает два стиля: конфигурационный и JSX.

## Конфигурационный стиль

```ts
const router = createGuardedRouter(
  [
    { path: '/', element: <HomePage /> },
    { path: '/login', element: <LoginPage />, access: 'guest-only' },
    { path: '/dashboard', element: <DashboardPage />, access: 'authenticated' },
    { path: '/admin', element: <AdminPage />, access: 'authenticated', roles: ['admin'] },
    {
      path: '/contracts',
      element: <ContractsPage />,
      access: 'authenticated',
      permissions: ['contracts:read'],
    },
  ],
  {
    getUser: () => useAuthStore.getState().user,
    hasRole: (user, roles) => roles.some((role) => user.roles.includes(role)),
    hasPermission: (user, permissions) =>
      permissions.every((p) => user.permissions.includes(p)),
    loginPath: '/login',
    forbiddenPath: '/403',
  }
)
```

## JSX стиль

```tsx
<GuardProvider
  getUser={() => useAuthStore.getState().user}
  hasRole={(user, roles) => roles.some((role) => user.roles.includes(role))}
  loginPath="/login"
  forbiddenPath="/403"
>
  <Routes>
    <Route path="/" element={<HomePage />} />
    <Route
      path="/dashboard"
      element={
        <GuardRoute access="authenticated">
          <DashboardPage />
        </GuardRoute>
      }
    />
  </Routes>
</GuardProvider>
```

## Роли, привилегии или оба варианта

RBAC — передаёте `roles` в конфигурацию роута. Логику проверки определяете сами: OR, AND, иерархия — как требует ваш домен.

ABAC — передаёте `permissions`. Работает по тому же принципу.

Оба сразу — проверка проходит только когда выполнены оба условия.

## Не используете React Router?

Используйте `@react-protected/core` напрямую. Пример с TanStack Router:

```ts
const guard = createGuard({
  getUser: () => useAuthStore.getState().user,
  hasRole: (user, roles) => roles.some((role) => user.roles.includes(role)),
})

const dashboardRoute = createRoute({
  path: '/dashboard',
  beforeLoad: ({ location }) => {
    const result = guard.check(
      { path: '/dashboard', access: 'authenticated' },
      location.pathname
    )
    if (!result.allowed) throw redirect({ to: result.redirectTo })
  },
  component: DashboardPage,
})
```

[github.com/astakhovaskold/react-protected](https://github.com/astakhovaskold/react-protected)

Вопросы и звёзды приветствуются.
